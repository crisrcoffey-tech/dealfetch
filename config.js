// Supabase client config. Replace the placeholders below with the values
// from your Supabase project (Settings → API).
//
// The anon key is safe to expose in client-side code — it only permits
// operations allowed by your Row Level Security policies, which are set
// up by SUPABASE_SETUP.md.
//
// See config.example.js and SUPABASE_SETUP.md for the full walkthrough.

window.SUPABASE_URL = "PASTE_SUPABASE_PROJECT_URL_HERE";
window.SUPABASE_ANON_KEY = "PASTE_SUPABASE_ANON_KEY_HERE";
