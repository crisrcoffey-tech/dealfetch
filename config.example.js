// Copy this file to config.js and fill in real values from
// your Supabase project (Settings → API).
//
// The anon key is safe to expose in client code — it only
// permits operations allowed by your Row Level Security policies.
//
// config.js is gitignored by default. For Vercel deploys you have
// two options:
//   (a) Remove config.js from .gitignore and commit it (simplest).
//   (b) Wire env vars and a small build step (overkill for static HTML).
//
// See SUPABASE_SETUP.md for the full setup walkthrough.

window.SUPABASE_URL = "PASTE_SUPABASE_PROJECT_URL_HERE";
window.SUPABASE_ANON_KEY = "PASTE_SUPABASE_ANON_KEY_HERE";
